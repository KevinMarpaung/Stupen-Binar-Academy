const data = { nama: "kevin marpaung", nim: "210504044", ip: 3.2 };
data.alamat = "sumut";

console.log(
  `nama saya adalah ${data.nama} dan nim saya ${data.nim} dan ip semester saya ${data.ip} alamat asal saya dari ${data.alamat}`
);
